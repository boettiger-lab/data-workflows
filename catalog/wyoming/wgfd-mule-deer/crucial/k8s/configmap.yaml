# Auto-generated ConfigMap containing job definitions
# Generated from: wgfd-mule-deer-crucial-setup-bucket.yaml, wgfd-mule-deer-crucial-convert.yaml, wgfd-mule-deer-crucial-pmtiles.yaml, wgfd-mule-deer-crucial-hex.yaml, wgfd-mule-deer-crucial-repartition.yaml
# Generation command: cng-datasets workflow --dataset wgfd-mule-deer-crucial --source-url s3://public-wyoming/raw/MuleDeerCrucialRange_-6928355498246013770.geojson --bucket public-wyoming --h3-resolution 10 --parent-resolutions "9,8,0"
#
# This ConfigMap is equivalent to running:
#   kubectl create configmap wgfd-mule-deer-crucial-yamls \
#     --from-file=wgfd-mule-deer-crucial-setup-bucket.yaml \
#     --from-file=wgfd-mule-deer-crucial-convert.yaml \
#     --from-file=wgfd-mule-deer-crucial-pmtiles.yaml \
#     --from-file=wgfd-mule-deer-crucial-hex.yaml \
#     --from-file=wgfd-mule-deer-crucial-repartition.yaml
#
# To update: regenerate workflow with cng-datasets and re-apply with kubectl apply -f configmap.yaml
apiVersion: v1
data:
  wgfd-mule-deer-crucial-convert.yaml: |
    apiVersion: batch/v1
    kind: Job
    metadata:
      name: wgfd-mule-deer-crucial-convert
      labels:
        k8s-app: wgfd-mule-deer-crucial-convert
    spec:
      completions: 1
      parallelism: 1
      backoffLimit: 1
      ttlSecondsAfterFinished: 10800
      template:
        metadata:
          labels:
            k8s-app: wgfd-mule-deer-crucial-convert
        spec:
          priorityClassName: opportunistic
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: feature.node.kubernetes.io/pci-10de.present
                    operator: NotIn
                    values:
                    - 'true'
          restartPolicy: Never
          containers:
          - name: convert-task
            image: ghcr.io/boettiger-lab/datasets:latest
            imagePullPolicy: Always
            env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_S3_ENDPOINT
              value: rook-ceph-rgw-nautiluss3.rook
            - name: AWS_PUBLIC_ENDPOINT
              value: s3-west.nrp-nautilus.io
            - name: AWS_HTTPS
              value: 'false'
            - name: AWS_VIRTUAL_HOSTING
              value: 'FALSE'
            - name: BUCKET
              value: public-wyoming
            volumeMounts:
            - name: rclone-config
              mountPath: /root/.config/rclone
              readOnly: true
            command:
            - bash
            - -c
            - |
              set -e
              cng-convert-to-parquet \
                https://s3-west.nrp-nautilus.io/public-wyoming/raw/MuleDeerCrucialRange_-6928355498246013770.geojson \
                s3://public-wyoming/wgfd-mule-deer-crucial.parquet \
                --row-group-size 100000
            resources:
              requests:
                cpu: '4'
                memory: 8Gi
              limits:
                cpu: '4'
                memory: 8Gi
          volumes:
          - name: rclone-config
            secret:
              secretName: rclone-config
  wgfd-mule-deer-crucial-hex.yaml: |
    apiVersion: batch/v1
    kind: Job
    metadata:
      name: wgfd-mule-deer-crucial-hex
      labels:
        k8s-app: wgfd-mule-deer-crucial-hex
    spec:
      completions: 145
      parallelism: 50
      completionMode: Indexed
      backoffLimit: 0
      podFailurePolicy:
        rules:
        - action: Ignore
          onPodConditions:
          - type: DisruptionTarget
      ttlSecondsAfterFinished: 10800
      template:
        metadata:
          labels:
            k8s-app: wgfd-mule-deer-crucial-hex
        spec:
          priorityClassName: opportunistic
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: feature.node.kubernetes.io/pci-10de.present
                    operator: NotIn
                    values:
                    - 'true'
          restartPolicy: Never
          containers:
          - name: hex-task
            image: ghcr.io/boettiger-lab/datasets:latest
            imagePullPolicy: Always
            env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_S3_ENDPOINT
              value: rook-ceph-rgw-nautiluss3.rook
            - name: AWS_PUBLIC_ENDPOINT
              value: s3-west.nrp-nautilus.io
            - name: AWS_HTTPS
              value: 'false'
            - name: AWS_VIRTUAL_HOSTING
              value: 'FALSE'
            - name: TMPDIR
              value: /tmp
            - name: BUCKET
              value: public-wyoming
            - name: DATASET
              value: wgfd-mule-deer-crucial
            command:
            - bash
            - -c
            - |-
              set -e
              cng-datasets vector --input s3://public-wyoming/wgfd-mule-deer-crucial.parquet --output s3://public-wyoming/wgfd-mule-deer-crucial/chunks --chunk-id ${JOB_COMPLETION_INDEX} --chunk-size 1 --intermediate-chunk-size 10 --resolution 10 --parent-resolutions 9,8,0
            resources:
              requests:
                cpu: '4'
                memory: 8Gi
              limits:
                cpu: '4'
                memory: 8Gi
  wgfd-mule-deer-crucial-pmtiles.yaml: |
    apiVersion: batch/v1
    kind: Job
    metadata:
      name: wgfd-mule-deer-crucial-pmtiles
      labels:
        k8s-app: wgfd-mule-deer-crucial-pmtiles
    spec:
      completions: 1
      parallelism: 1
      backoffLimit: 1
      ttlSecondsAfterFinished: 10800
      template:
        metadata:
          labels:
            k8s-app: wgfd-mule-deer-crucial-pmtiles
        spec:
          priorityClassName: opportunistic
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: feature.node.kubernetes.io/pci-10de.present
                    operator: NotIn
                    values:
                    - 'true'
          restartPolicy: Never
          containers:
          - name: pmtiles-task
            image: ghcr.io/boettiger-lab/datasets:latest
            imagePullPolicy: Always
            env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_S3_ENDPOINT
              value: rook-ceph-rgw-nautiluss3.rook
            - name: AWS_PUBLIC_ENDPOINT
              value: s3-west.nrp-nautilus.io
            - name: AWS_HTTPS
              value: 'false'
            - name: AWS_VIRTUAL_HOSTING
              value: 'FALSE'
            - name: TMPDIR
              value: /tmp
            - name: BUCKET
              value: public-wyoming
            - name: DATASET
              value: wgfd-mule-deer-crucial
            volumeMounts:
            - name: rclone-config
              mountPath: /root/.config/rclone
              readOnly: true
            command:
            - bash
            - -c
            - |
              set -e
              # Use optimized GeoParquet (has ID column) from convert job
              # GDAL can read parquet via vsicurl
              ogr2ogr -wrapdateline -datelineoffset 15 -f GeoJSONSeq /tmp/$DATASET.geojsonl /vsicurl/https://s3-west.nrp-nautilus.io/public-wyoming/wgfd-mule-deer-crucial.parquet -progress

              # Generate PMTiles from GeoJSONSeq
              tippecanoe -o /tmp/$DATASET.pmtiles -l $DATASET --drop-densest-as-needed --extend-zooms-if-still-dropping --force /tmp/$DATASET.geojsonl

              # Upload to S3 using rclone
              rclone copy /tmp/$DATASET.pmtiles nrp:public-wyoming/
              rm /tmp/$DATASET.geojsonl /tmp/$DATASET.pmtiles
            resources:
              requests:
                cpu: '4'
                memory: 8Gi
              limits:
                cpu: '4'
                memory: 8Gi
          volumes:
          - name: rclone-config
            secret:
              secretName: rclone-config
  wgfd-mule-deer-crucial-repartition.yaml: |
    apiVersion: batch/v1
    kind: Job
    metadata:
      name: wgfd-mule-deer-crucial-repartition
      labels:
        k8s-app: wgfd-mule-deer-crucial-repartition
    spec:
      completions: 1
      parallelism: 1
      backoffLimit: 1
      ttlSecondsAfterFinished: 10800
      template:
        metadata:
          labels:
            k8s-app: wgfd-mule-deer-crucial-repartition
        spec:
          priorityClassName: opportunistic
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: feature.node.kubernetes.io/pci-10de.present
                    operator: NotIn
                    values:
                    - 'true'
          restartPolicy: Never
          containers:
          - name: repartition-task
            image: ghcr.io/boettiger-lab/datasets:latest
            imagePullPolicy: Always
            env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_S3_ENDPOINT
              value: rook-ceph-rgw-nautiluss3.rook
            - name: AWS_PUBLIC_ENDPOINT
              value: s3-west.nrp-nautilus.io
            - name: AWS_HTTPS
              value: 'false'
            - name: AWS_VIRTUAL_HOSTING
              value: 'FALSE'
            - name: TMPDIR
              value: /tmp
            - name: BUCKET
              value: public-wyoming
            volumeMounts:
            - name: rclone-config
              mountPath: /root/.config/rclone
              readOnly: true
            command:
            - bash
            - -c
            - |
              set -e
              cng-datasets repartition --chunks-dir s3://public-wyoming/wgfd-mule-deer-crucial/chunks --output-dir s3://public-wyoming/wgfd-mule-deer-crucial/hex --source-parquet s3://public-wyoming/wgfd-mule-deer-crucial.parquet --cleanup
            resources:
              requests:
                cpu: '4'
                memory: 32Gi
              limits:
                cpu: '4'
                memory: 32Gi
          volumes:
          - name: rclone-config
            secret:
              secretName: rclone-config
  wgfd-mule-deer-crucial-setup-bucket.yaml: |
    apiVersion: batch/v1
    kind: Job
    metadata:
      name: wgfd-mule-deer-crucial-setup-bucket
      labels:
        k8s-app: wgfd-mule-deer-crucial-setup-bucket
    spec:
      completions: 1
      parallelism: 1
      backoffLimit: 2
      ttlSecondsAfterFinished: 10800
      template:
        metadata:
          labels:
            k8s-app: wgfd-mule-deer-crucial-setup-bucket
        spec:
          priorityClassName: opportunistic
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: feature.node.kubernetes.io/pci-10de.present
                    operator: NotIn
                    values:
                    - 'true'
          restartPolicy: Never
          containers:
          - name: setup-bucket-task
            image: ghcr.io/boettiger-lab/datasets:latest
            imagePullPolicy: Always
            env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_S3_ENDPOINT
              value: rook-ceph-rgw-nautiluss3.rook
            - name: AWS_PUBLIC_ENDPOINT
              value: s3-west.nrp-nautilus.io
            - name: AWS_HTTPS
              value: 'false'
            - name: AWS_VIRTUAL_HOSTING
              value: 'FALSE'
            - name: BUCKET
              value: public-wyoming
            volumeMounts:
            - name: rclone-config
              mountPath: /root/.config/rclone
              readOnly: true
            command:
            - bash
            - -c
            - |
              set -e
              echo "Setting up bucket with public access and CORS..."
              cng-datasets storage setup-bucket \
                --bucket "${BUCKET}" \
                --remote nrp \
                --verify

              echo "Bucket setup complete!"
            resources:
              requests:
                cpu: '1'
                memory: 2Gi
              limits:
                cpu: '1'
                memory: 2Gi
          volumes:
          - name: rclone-config
            secret:
              secretName: rclone-config
kind: ConfigMap
metadata:
  name: wgfd-mule-deer-crucial-yamls
  namespace: biodiversity

apiVersion: batch/v1
kind: Job
metadata:
  name: crop-irrigated-workflow
  namespace: biodiversity
spec:
  template:
    spec:
      containers:
      - args:
        - "set -e\n\necho \"Starting crop-irrigated raster workflow...\"\n\n# Step\
          \ 0: Setup bucket\necho \"Step 0: Setting up bucket...\"\nkubectl apply\
          \ -f /yamls/crop-irrigated-setup-bucket.yaml -n biodiversity\nkubectl wait\
          \ --for=condition=complete --timeout=600s job/crop-irrigated-setup-bucket\
          \ -n biodiversity\n\n# Step 1: H3 hexagonal tiling\necho \"Step 1: H3 hexagonal\
          \ tiling...\"\nkubectl apply -f /yamls/crop-irrigated-hex.yaml -n biodiversity\n\
          kubectl wait --for=condition=complete --timeout=7200s job/crop-irrigated-hex\
          \ -n biodiversity\n\necho \"\u2713 Workflow complete!\"\necho \"Clean up\
          \ with:\"\necho \"  kubectl delete jobs crop-irrigated-setup-bucket crop-irrigated-hex\
          \ crop-irrigated-workflow -n biodiversity\"\necho \"  kubectl delete configmap\
          \ crop-irrigated-yamls -n biodiversity\"\n"
        command:
        - bash
        - -c
        image: bitnami/kubectl:latest
        name: workflow
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - mountPath: /yamls
          name: yamls
      restartPolicy: OnFailure
      serviceAccountName: cng-datasets-workflow
      volumes:
      - configMap:
          name: crop-irrigated-yamls
        name: yamls
  ttlSecondsAfterFinished: 10800

apiVersion: batch/v1
kind: Job
metadata:
  name: wetlands-ramsar-reencode-hex
  namespace: biodiversity
  labels:
    k8s-app: wetlands-ramsar-reencode-hex
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 10800
  template:
    metadata:
      labels:
        k8s-app: wetlands-ramsar-reencode-hex
    spec:
      priorityClassName: opportunistic
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: feature.node.kubernetes.io/pci-10de.present
                operator: NotIn
                values:
                - 'true'
      containers:
      - name: reencode
        image: ghcr.io/boettiger-lab/datasets:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: '4'
            memory: 16Gi
            ephemeral-storage: 10Gi
          limits:
            cpu: '4'
            memory: 16Gi
        env:
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws
              key: AWS_SECRET_ACCESS_KEY
        - name: AWS_S3_ENDPOINT
          value: rook-ceph-rgw-nautiluss3.rook
        - name: AWS_PUBLIC_ENDPOINT
          value: s3-west.nrp-nautilus.io
        - name: AWS_HTTPS
          value: 'false'
        - name: AWS_VIRTUAL_HOSTING
          value: 'FALSE'
        volumeMounts:
        - name: rclone-config
          mountPath: /root/.config/rclone
          readOnly: true
        command:
        - bash
        - -c
        - |-
          set -e
          echo "=== Re-encoding wetlands-ramsar-reencode-hex: VARCHAR H3 -> UBIGINT ==="

          python3 - <<'PYEOF'
          import duckdb, os
          
          con = duckdb.connect()
          con.execute("INSTALL h3 FROM community; LOAD h3")
          con.execute("INSTALL httpfs; LOAD httpfs")
          con.execute("SET http_retries=10; SET http_retry_wait_ms=3000")
          
          endpoint = os.environ.get("AWS_S3_ENDPOINT", "rook-ceph-rgw-nautiluss3.rook")
          key = os.environ["AWS_ACCESS_KEY_ID"]
          secret = os.environ["AWS_SECRET_ACCESS_KEY"]
          
          con.execute(f"""
              CREATE SECRET s3_secret (
                  TYPE S3, KEY_ID '{key}', SECRET '{secret}',
                  ENDPOINT '{endpoint}', URL_STYLE 'path', USE_SSL false
              )
          """)
          
          print("Converting...")
          con.execute("""
              COPY (
                  SELECT * EXCLUDE (h1, h2, h3, h4, h5, h6, h7, h8, h9, h0), h3_string_to_h3(h1) AS h1, h3_string_to_h3(h2) AS h2, h3_string_to_h3(h3) AS h3, h3_string_to_h3(h4) AS h4, h3_string_to_h3(h5) AS h5, h3_string_to_h3(h6) AS h6, h3_string_to_h3(h7) AS h7, h3_string_to_h3(h8) AS h8, h3_string_to_h3(h9) AS h9, h3_string_to_h3(h0) AS h0
                  FROM read_parquet('s3://public-wetlands/ramsar/hex/**/*.parquet')
              ) TO 's3://public-wetlands/ramsar/hex-new/'
              (FORMAT PARQUET, COMPRESSION ZSTD, PARTITION_BY (h0))
          """)
          print("Done.")
          PYEOF

          N=$(rclone ls nrp:public-wetlands/ramsar/hex-new/ | wc -l)
          echo "  ${N} files in output"
          if [ "$N" -lt 1 ]; then echo "ERROR: no output files"; exit 1; fi

          SRC_DIR=$(echo "s3://public-wetlands/ramsar/hex/**/*.parquet" | sed 's|/\*\*.*||')
          SRC_RCLONE=$(echo "$SRC_DIR" | sed 's|s3://|nrp:|')
          echo "Swapping $SRC_RCLONE -> nrp:public-wetlands/ramsar/hex/hex/ ..."
          rclone delete "$SRC_RCLONE/"
          rclone move nrp:public-wetlands/ramsar/hex-new/ "${SRC_RCLONE}/"
          echo "=== Complete ==="
      volumes:
      - name: rclone-config
        secret:
          secretName: rclone-config

apiVersion: batch/v1
kind: Job
metadata:
  name: oil-shale-leases-workflow
  namespace: geo-workflows
spec:
  template:
    spec:
      containers:
      - args:
        - "\nset -e\n\necho \"Starting oil-shale-leases workflow...\"\n\n# Step 0:\
          \ Setup bucket with public access and CORS\necho \"Step 0: Setting up bucket...\"\
          \nkubectl apply -f /yamls/oil-shale-leases-setup-bucket.yaml -n geo-workflows\n\
          \necho \"Waiting for bucket setup to complete...\"\nkubectl wait --for=condition=complete\
          \ --timeout=600s job/oil-shale-leases-setup-bucket -n geo-workflows\n\n\
          # Step 1: Convert to optimized GeoParquet (needed by both pmtiles and hex\
          \ jobs)\necho \"Step 1: Converting to optimized GeoParquet...\"\nkubectl\
          \ apply -f /yamls/oil-shale-leases-convert.yaml -n geo-workflows\n\necho\
          \ \"Waiting for GeoParquet conversion to complete...\"\nkubectl wait --for=condition=complete\
          \ --timeout=3600s job/oil-shale-leases-convert -n geo-workflows\n\n# Step\
          \ 2: Run pmtiles and hex tiling in parallel (both use the converted geoparquet)\n\
          echo \"Step 2: H3 hexagonal tiling and PMTiles generation (parallel)...\"\
          \nkubectl apply -f /yamls/oil-shale-leases-pmtiles.yaml -n geo-workflows\n\
          kubectl apply -f /yamls/oil-shale-leases-hex.yaml -n geo-workflows\n\necho\
          \ \"Waiting for hex tiling to complete (PMTiles continues in background)...\"\
          \necho \"Timeout set to 48 hours (172800s)...\"\nkubectl wait --for=condition=complete\
          \ --timeout=172800s job/oil-shale-leases-hex -n geo-workflows\n\necho \"\
          Step 3: Repartitioning by h0...\"\nkubectl apply -f /yamls/oil-shale-leases-repartition.yaml\
          \ -n geo-workflows\n\necho \"Waiting for repartition to complete...\"\n\
          kubectl wait --for=condition=complete --timeout=3600s job/oil-shale-leases-repartition\
          \ -n geo-workflows\n\necho \"\u2713 Workflow complete!\"\necho \"Note: PMTiles\
          \ job may still be running in the background\"\necho \"\"\necho \"Clean\
          \ up with:\"\necho \"  kubectl delete jobs oil-shale-leases-setup-bucket\
          \ oil-shale-leases-convert oil-shale-leases-pmtiles oil-shale-leases-hex\
          \ oil-shale-leases-repartition oil-shale-leases-workflow -n geo-workflows\"\
          \necho \"  kubectl delete configmap oil-shale-leases-yamls -n geo-workflows\"\
          \n"
        command:
        - bash
        - -c
        image: bitnami/kubectl:latest
        name: workflow
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - mountPath: /yamls
          name: yamls
      restartPolicy: OnFailure
      serviceAccountName: cng-datasets-workflow
      volumes:
      - configMap:
          name: oil-shale-leases-yamls
        name: yamls
  ttlSecondsAfterFinished: 10800

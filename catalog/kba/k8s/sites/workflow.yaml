apiVersion: batch/v1
kind: Job
metadata:
  name: kba-2026-03-sites-workflow
  namespace: geo-workflows
spec:
  template:
    spec:
      containers:
      - args:
        - "\nset -e\n\necho \"Starting kba-2026-03-sites workflow...\"\n\n# Step 0:\
          \ Setup bucket with public access and CORS\necho \"Step 0: Setting up bucket...\"\
          \nkubectl apply -f /yamls/kba-2026-03-sites-setup-bucket.yaml -n geo-workflows\n\
          \necho \"Waiting for bucket setup to complete...\"\nkubectl wait --for=condition=complete\
          \ --timeout=600s job/kba-2026-03-sites-setup-bucket -n geo-workflows\n\n\
          # Step 1: Convert to optimized GeoParquet (needed by both pmtiles and hex\
          \ jobs)\necho \"Step 1: Converting to optimized GeoParquet...\"\nkubectl\
          \ apply -f /yamls/kba-2026-03-sites-convert.yaml -n geo-workflows\n\necho\
          \ \"Waiting for GeoParquet conversion to complete...\"\nkubectl wait --for=condition=complete\
          \ --timeout=3600s job/kba-2026-03-sites-convert -n geo-workflows\n\n# Step\
          \ 2: Run pmtiles and hex tiling in parallel (both use the converted geoparquet)\n\
          echo \"Step 2: H3 hexagonal tiling and PMTiles generation (parallel)...\"\
          \nkubectl apply -f /yamls/kba-2026-03-sites-pmtiles.yaml -n geo-workflows\n\
          kubectl apply -f /yamls/kba-2026-03-sites-hex.yaml -n geo-workflows\n\n\
          echo \"Waiting for hex tiling to complete (PMTiles continues in background)...\"\
          \necho \"Timeout set to 48 hours (172800s)...\"\nkubectl wait --for=condition=complete\
          \ --timeout=172800s job/kba-2026-03-sites-hex -n geo-workflows\n\necho \"\
          Step 3: Repartitioning by h0...\"\nkubectl apply -f /yamls/kba-2026-03-sites-repartition.yaml\
          \ -n geo-workflows\n\necho \"Waiting for repartition to complete...\"\n\
          kubectl wait --for=condition=complete --timeout=3600s job/kba-2026-03-sites-repartition\
          \ -n geo-workflows\n\necho \"\u2713 Workflow complete!\"\necho \"Note: PMTiles\
          \ job may still be running in the background\"\necho \"\"\necho \"Clean\
          \ up with:\"\necho \"  kubectl delete jobs kba-2026-03-sites-setup-bucket\
          \ kba-2026-03-sites-convert kba-2026-03-sites-pmtiles kba-2026-03-sites-hex\
          \ kba-2026-03-sites-repartition kba-2026-03-sites-workflow -n geo-workflows\"\
          \necho \"  kubectl delete configmap kba-2026-03-sites-yamls -n geo-workflows\"\
          \n"
        command:
        - bash
        - -c
        image: bitnami/kubectl:latest
        name: workflow
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - mountPath: /yamls
          name: yamls
      restartPolicy: OnFailure
      serviceAccountName: cng-datasets-workflow
      volumes:
      - configMap:
          name: kba-2026-03-sites-yamls
        name: yamls
  ttlSecondsAfterFinished: 10800

# One-cell hex TEST (h0 index 96, a Peru data cell) to validate the res-10
# exact_extract path on the 64 GB COG before running all Peru cells.
# The 64 GB COG exceeds the 50Gi ephemeral cap, so it localizes to the shared
# rechunk-scratch PVC (per-pod cache subdir). nodata (-9999) is embedded in the
# COG, so exact_extract excludes masked (non-Peru) pixels automatically.
apiVersion: batch/v1
kind: Job
metadata:
  name: fdap-2025b-cocoa-peru-2024-hex-test
  labels: {k8s-app: fdap-2025b-cocoa-peru-2024-hex-test}
spec:
  completions: 1
  parallelism: 1
  completionMode: Indexed
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels: {k8s-app: fdap-2025b-cocoa-peru-2024-hex-test}
    spec:
      restartPolicy: Never
      activeDeadlineSeconds: 10800
      containers:
      - name: hex-task
        image: ghcr.io/boettiger-lab/datasets:latest
        imagePullPolicy: Always
        env:
        - {name: AWS_ACCESS_KEY_ID, valueFrom: {secretKeyRef: {name: aws, key: AWS_ACCESS_KEY_ID}}}
        - {name: AWS_SECRET_ACCESS_KEY, valueFrom: {secretKeyRef: {name: aws, key: AWS_SECRET_ACCESS_KEY}}}
        - {name: AWS_S3_ENDPOINT, value: rook-ceph-rgw-nautiluss3.rook}
        - {name: AWS_PUBLIC_ENDPOINT, value: s3-west.nrp-nautilus.io}
        - {name: AWS_HTTPS, value: 'false'}
        - {name: AWS_VIRTUAL_HOSTING, value: 'FALSE'}
        - {name: GDAL_DATA, value: /usr/share/gdal}
        - {name: PYTHONPATH, value: /usr/lib/python3/dist-packages}
        - {name: TMPDIR, value: /scratch/tmp}
        - {name: CPL_TMPDIR, value: /scratch/tmp}
        volumeMounts:
        - {name: rclone-config, mountPath: /root/.config/rclone, readOnly: true}
        - {name: scratch, mountPath: /scratch, subPath: fdap-cocoa-peru-2024-hex}
        command:
        - bash
        - -c
        - |
          set -euo pipefail
          mkdir -p /scratch/tmp
          CHUNK_MAP=(96)
          H0=${CHUNK_MAP[$JOB_COMPLETION_INDEX]}
          echo "== hex TEST: h0-index $H0, res 10 =="
          cng-datasets raster \
            --input s3://public-commodities/raw/fdap-2025b-cocoa-peru-2024.tif \
            --output-parquet s3://public-commodities/fdap-2025b/cocoa-peru-2024/hex/ \
            --h0-index $H0 \
            --resolution 9 --parent-resolutions 8,0 \
            --value-column probability --hex-resampling mean \
            --nodata nan \
            --local-cache-dir /scratch/cache-${JOB_COMPLETION_INDEX}
          echo "== hex TEST complete =="
        resources:
          requests: {cpu: '8', memory: 64Gi, ephemeral-storage: 20Gi}
          limits: {cpu: '8', memory: 64Gi, ephemeral-storage: 20Gi}
      volumes:
      - {name: rclone-config, secret: {secretName: rclone-config}}
      - {name: scratch, persistentVolumeClaim: {claimName: rechunk-scratch}}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - {key: feature.node.kubernetes.io/pci-10de.present, operator: NotIn, values: ['true']}
              - {key: kubernetes.io/hostname, operator: NotIn, values: [hpc-nrp-g1.nmsu.edu, service-02.nrp.mghpcc.org]}

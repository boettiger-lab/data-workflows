apiVersion: batch/v1
kind: Job
metadata:
  name: om-2026-regions-repartition
  labels:
    k8s-app: om-2026-regions-repartition
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 1
  ttlSecondsAfterFinished: 10800
  template:
    metadata:
      labels:
        k8s-app: om-2026-regions-repartition
    spec:
      priorityClassName: opportunistic
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: feature.node.kubernetes.io/pci-10de.present
                operator: NotIn
                values:
                - 'true'
      restartPolicy: Never
      containers:
      - name: repartition-task
        image: ghcr.io/boettiger-lab/datasets:latest
        imagePullPolicy: Always
        env:
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws
              key: AWS_SECRET_ACCESS_KEY
        - name: AWS_S3_ENDPOINT
          value: rook-ceph-rgw-nautiluss3.rook
        - name: AWS_PUBLIC_ENDPOINT
          value: s3-west.nrp-nautilus.io
        - name: AWS_HTTPS
          value: 'false'
        - name: AWS_VIRTUAL_HOSTING
          value: 'FALSE'
        - name: TMPDIR
          value: /tmp
        - name: BUCKET
          value: public-overturemaps
        volumeMounts:
        - name: rclone-config
          mountPath: /root/.config/rclone
          readOnly: true
        command:
        - bash
        - -c
        - |
          set -e
          cng-datasets repartition --chunks-dir s3://public-overturemaps/2026-02-18.0/regions/chunks --output-dir s3://public-overturemaps/2026-02-18.0/regions/hex --source-parquet s3://public-overturemaps/2026-02-18.0/regions.parquet --cleanup --memory-limit 300GiB
        resources:
          requests:
            cpu: '8'
            memory: 350Gi
            ephemeral-storage: 200Gi
          limits:
            cpu: '8'
            memory: 350Gi
            ephemeral-storage: 200Gi
      volumes:
      - name: rclone-config
        secret:
          secretName: rclone-config

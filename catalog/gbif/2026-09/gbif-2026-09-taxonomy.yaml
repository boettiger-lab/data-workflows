# GBIF 2026-09 taxonomy aggregation (port of catalog/gbif/2026-06/gbif-2026-06-taxonomy.yaml).
# Per h0, GROUP BY the taxonomy columns + COUNT(*) -> 2026-09/taxonomy/h0=<int>/data_0.parquet.
# Required so the 2026-09 release matches the 2026-06 `taxonomic-aggregations` asset (#662).
# Discovers the h0 list from the actual hex via DuckDB glob (no hardcoded list, no boto3).
# Preemption: NO priorityClassName (default priority 0). The 2026-06 build ran these
# at `opportunistic` (-2000000000); pods over ~1 hour are a large preemption target and
# die repeatedly at that priority (skill `pod-preemption`, data-workflows#564).
# Parallelism is halved in trade, so the total claim on shared nodes does not grow.
apiVersion: batch/v1
kind: Job
metadata:
  name: gbif-2026-09-taxonomy
  namespace: geo-workflows
  labels:
    k8s-app: gbif-2026-09-taxonomy
spec:
  completions: 122
  parallelism: 20
  completionMode: Indexed
  backoffLimitPerIndex: 3
  maxFailedIndexes: 5
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        k8s-app: gbif-2026-09-taxonomy
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: feature.node.kubernetes.io/pci-10de.present
                operator: NotIn
                values: ['true']
      containers:
      - name: taxonomy
        image: ghcr.io/boettiger-lab/datasets:latest
        imagePullPolicy: Always
        resources:
          requests: {cpu: '4', memory: 16Gi, ephemeral-storage: 4Gi}
          limits:   {cpu: '4', memory: 16Gi, ephemeral-storage: 4Gi}
        env:
        - name: AWS_ACCESS_KEY_ID
          valueFrom: {secretKeyRef: {name: aws, key: AWS_ACCESS_KEY_ID}}
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom: {secretKeyRef: {name: aws, key: AWS_SECRET_ACCESS_KEY}}
        - name: AWS_S3_ENDPOINT
          value: rook-ceph-rgw-nautiluss3.rook
        - name: AWS_HTTPS
          value: 'false'
        - name: AWS_VIRTUAL_HOSTING
          value: 'FALSE'
        - name: JOB_COMPLETION_INDEX
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['batch.kubernetes.io/job-completion-index']
        command:
        - bash
        - -c
        - |-
          set -euo pipefail
          python3 - <<'PYEOF'
          import duckdb, os, sys
          idx = int(os.environ["JOB_COMPLETION_INDEX"])
          key = os.environ["AWS_ACCESS_KEY_ID"]; secret = os.environ["AWS_SECRET_ACCESS_KEY"]
          endpoint = os.environ["AWS_S3_ENDPOINT"]
          con = duckdb.connect()
          con.execute("INSTALL httpfs; LOAD httpfs")
          con.execute("SET http_retries=10; SET http_retry_wait_ms=3000")
          con.execute(f"CREATE SECRET s3_secret (TYPE S3, KEY_ID '{key}', SECRET '{secret}', ENDPOINT '{endpoint}', URL_STYLE 'path', USE_SSL false)")
          h0s = [r[0] for r in con.execute(
              "SELECT DISTINCT regexp_extract(file,'h0=([0-9]+)',1) AS h0 "
              "FROM glob('s3://public-gbif/2026-09/hex/h0=*/data_0.parquet') ORDER BY h0").fetchall()]
          if idx >= len(h0s):
              print(f"idx {idx} >= {len(h0s)} partitions; nothing to do"); sys.exit(0)
          h0 = h0s[idx]
          src = f"s3://public-gbif/2026-09/hex/h0={h0}/data_0.parquet"
          dst = f"s3://public-gbif/2026-09/taxonomy/h0={h0}/data_0.parquet"
          print(f"idx {idx}: {src} -> {dst}", flush=True)
          con.execute(f"""
              COPY (
                  SELECT kingdom, phylum, class, "order", family,
                         genus, species, infraspecificepithet,
                         taxonrank, scientificname,
                         verbatimscientificname, verbatimscientificnameauthorship,
                         COUNT(*) AS n,
                         {h0}::UBIGINT AS h0
                  FROM read_parquet('{src}')
                  GROUP BY ALL
              ) TO '{dst}' (FORMAT PARQUET, COMPRESSION ZSTD, ROW_GROUP_SIZE 1000000)
          """)
          print(f"idx {idx}: done", flush=True)
          PYEOF
